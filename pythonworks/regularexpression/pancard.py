# first three characters must be any random uppercase alphabet
# in the 4th place  P,F,A,C,H,T
# in the 5th place uppercase random alphabet
# 4digit
# in last place one random alphabet

# from re import*
# pan_id=input("enter pan card")
# rule="[A-Z]{3}[P,F,A,C,T,H][A-Z][\d]{4}[A-Za-z]*"
# matcher=fullmatch(rule,pan_id)
# print("invalid" if matcher==None else "valid")




